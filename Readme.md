# This project can be use for solo mining on pools like solo.ckpool.org

### setup instruction

1) clone the repository
2) cd project
3) cargo run build --release
4) ./target/release/miner-project --address "solo.ckpool.org:3333" --wallet "bitcoin address"